{% extends 'base.html.twig' %}

{% block title %}Edit Worker{% endblock %}

{% block left_sidebar %}
	{% include 'area/common/left-sidebar.twig' with { active: 'ge-list'} %}
{% endblock %}

{% block body %}
    {# <h1>Edit Worker</h1> #}

    {{ include('worker/_form.html.twig', {'button_label': 'Update'}) }}

    {# <a href="{{ path('worker_index') }}">back to list</a> #}

    {# {{ include('worker/_delete_form.html.twig') }} #}
{% endblock %}
