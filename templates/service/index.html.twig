{% extends 'base.html.twig' %}

{% block title %}Service index{% endblock %}

{% block left_sidebar %}
	{% include 'area/common/left-sidebar.twig' with { active: 'service-list'} %}
{% endblock %}

{% block body %}
    <table id="societeDatatable" class="table table-bordered table-striped">
        <thead>
            <tr>
                {# <th>Id</th> #}
                <th>Nom</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
        {% for service in services %}
            <tr>
                {# <td>{{ service.id }}</td> #}
                <td>{{ service.name }}</td>
                <td>
                    {# <a href="{{ path('societe_show', {'id': service.id}) }}">show</a> #}
                    <a href="{{ path('service_edit', {'id': service.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <a href="{{ path('societe_new') }}">Ajouter une société</a> #}
{% endblock %}
{% block js %}
	<script>
$("#societeDatatable").DataTable({

"searching": true,
"order": [
[0, "DESC"]
],
"responsive": true,
"autoWidth": false
});
	</script>
{% endblock %}