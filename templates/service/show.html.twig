{% extends 'base.html.twig' %}

{% block title %}service{% endblock %}

{% block left_sidebar %}
	{% include 'area/common/left-sidebar.twig' with { active: 'service-list'} %}
{% endblock %}

{% block body %}
    <h1>Service</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ service.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('societe_index') }}">Retour à la liste</a>

    <a href="{{ path('societe_edit', {'id': service.id}) }}">Modifier</a>

    {{ include('service/_delete_form.html.twig') }}
{% endblock %}
