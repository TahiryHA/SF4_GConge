<form method="post" action="{{ path('gestion_conge_delete', {'id': gestion_conge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gestion_conge.id) }}">
    <button class="btn">Delete</button>
</form>
